version: 2.1

orbs:
  win: circleci/windows@2.2.0

jobs:
  build:
    executor: win/default

    steps:
      - checkout
      - run: ls
      - run: cd TProject1/
      - run: ls
      - run: chmod +x cm
      - run: ./cm selenoid start --vnc
      - run: dotnet build